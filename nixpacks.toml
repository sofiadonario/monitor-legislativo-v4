# nixpacks.toml

# Force the environment to use Python 3.11.
[providers]
python = "3.11"

# Define the installation and startup commands.
[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "uvicorn src.main:app --host 0.0.0.0 --port $PORT"