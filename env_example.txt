# Monitor Legislativo v4 - OAuth2 Environment Configuration
# Copy this file to .env and fill in your actual values
# DO NOT commit .env file to version control

# ========================================
# OAuth2 Authentication Configuration
# ========================================

# Google OAuth2 (Required for Google Academic login)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Microsoft OAuth2 (Required for Microsoft Academic login)
MICROSOFT_CLIENT_ID=your_microsoft_client_id_here
MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret_here
MICROSOFT_TENANT_ID=common

# ========================================
# Application Configuration
# ========================================

# Application URL (Railway will provide this)
APP_URL=https://your-app-name.up.railway.app

# Environment (production for Railway deployment)
R_CONFIG_ACTIVE=production

# Port (Railway will set this automatically)
PORT=3838

# ========================================
# Database Configuration
# ========================================

# Railway PostgreSQL (Railway will set this automatically)
DATABASE_URL=postgresql://user:password@host:port/database

# Alternative individual database settings (if not using DATABASE_URL)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=monitor_legislativo
DATABASE_USER=postgres
DATABASE_PASSWORD=your_password

# ========================================
# Security Configuration
# ========================================

# JWT Secret for session tokens (generate a random 64-character string)
JWT_SECRET=your_random_jwt_secret_64_characters_long_here

# Session encryption key (generate a random 32-character string)
SESSION_SECRET=your_random_session_secret_32_chars

# ========================================
# LGPD Compliance Configuration
# ========================================

# Contact emails for LGPD compliance
LGPD_CONTACT_EMAIL=privacidade@mackenzie.br
LGPD_DPO_EMAIL=dpo@mackenzie.br

# Institution information
INSTITUTION_NAME=Universidade Presbiteriana Mackenzie
INSTITUTION_ADDRESS=Rua da Consolação, 930, São Paulo-SP

# ========================================
# Optional: Redis Configuration
# ========================================

# Redis for caching (optional - if not set, uses memory cache)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# ========================================
# Optional: Email Configuration
# ========================================

# SMTP settings for sending LGPD notifications (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM=Monitor Legislativo <noreply@mackenzie.br>

# ========================================
# Optional: Analytics Configuration
# ========================================

# Google Analytics for usage tracking (optional)
GA_TRACKING_ID=UA-XXXXXXX-X

# ========================================
# Development Settings (Local Development Only)
# ========================================

# Uncomment for local development
# R_CONFIG_ACTIVE=development
# APP_URL=http://localhost:3838